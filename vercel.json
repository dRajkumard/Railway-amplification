{
    "version":2,
    "env":{
        "DB_URL":"postgres://admin:admin@localhost:5432"

    },
    "builds":[
        {
            "src":"src/main.ts",
            "use":"@vercel/node"
          
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/"
        }
    ]
}