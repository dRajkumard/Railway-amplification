{
    "version":2,
    "builds":[
        {
            "src":"./src/main.ts",
            "use":"@vercel/node",
            "installCommand": "pnpm install"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/"
        }
    ]
}